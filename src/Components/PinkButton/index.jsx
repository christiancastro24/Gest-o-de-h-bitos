import PinkButtonContainer from "./style"

const PinkButton = ({text}) => {
    return (
        <PinkButtonContainer>
            {text}
        </PinkButtonContainer>
    )
}

export default PinkButton;